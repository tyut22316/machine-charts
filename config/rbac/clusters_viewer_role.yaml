# permissions for end users to view clusters.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: clusters-viewer-role
rules:
- apiGroups:
  - machart.machart.rd
  resources:
  - clusters
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - machart.machart.rd
  resources:
  - clusters/status
  verbs:
  - get
